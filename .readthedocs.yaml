# Read the Docs configuration file for Sphinx projects
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

# Required
version: 2

# Set the OS, Python version and other tools you might need
build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
    # You can also specify other tool versions:
    # nodejs: "20"
    # rust: "1.70"
    # golang: "1.20"

  apt_packages: ["doxygen", "build-essential", "libfftw3-dev"]

  jobs:
    pre_build:
      # make编译库
      - cd pygrt/C_extension && make && cd -
      # 添加环境变量
      - export PATH="$(python -m pygrt.print | grep "PyGRT executable file directory" | awk '{print $NF}'):$PATH"
      # 进入docs/source/Tutorial，运行文档中的测试脚本，生成文档需要的文件和图
      - cd docs/source/Tutorial && chmod +x *.sh && ./run_all.sh && cd -
      # 初始化C和Python需要的.rst文件，使用doxygen编译C文档
      - cd docs && chmod +x *.sh && ./compile.sh && cd -


# Build documentation in the "docs/" directory with Sphinx
sphinx:
  configuration: docs/source/conf.py
  # You can configure Sphinx to use a different builder, for instance use the dirhtml builder for simpler URLs
  # builder: "dirhtml"
  # Fail on all warnings to avoid broken references
  # fail_on_warning: true

# Optionally build your docs in additional formats such as PDF and ePub
# formats:
#   - pdf
#   - epub

# Optional but recommended, declare the Python requirements required
# to build your documentation
# See https://docs.readthedocs.io/en/stable/guides/reproducible-builds.html
python:
  install:
    - requirements: docs/requirements.txt

  
