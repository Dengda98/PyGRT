**-Ds**\ *z1/z2/dz*\|\ *z1,z2,...*\|\ *file*
    震源深度列表 (km)。**-Ds** 和 **-Dr** 必须同时设置，并且不能与 **-D**
    同时使用。深度不能为负数，显式列出的深度必须严格递增。
    有三种设置方式：

    + **-Ds**\ *z1/z2/dz* - 在 [*z1*, *z2*] 范围内设置间隔为 *dz* 的一系列等距深度
    + **-Ds**\ *z1,z2,...* - 使用逗号分隔的多个震源深度
    + **-Ds**\ *file* - 从文件中读取震源深度，文件每行包含一个数值

**-Dr**\ *z1/z2/dz*\|\ *z1,z2,...*\|\ *file*
    台站或接收点深度列表 (km)。语法和限制与 **-Ds** 相同：

    + **-Dr**\ *z1/z2/dz* - 在 [*z1*, *z2*] 范围内设置间隔为 *dz* 的一系列等距深度
    + **-Dr**\ *z1,z2,...* - 使用逗号分隔的多个台站或接收点深度
    + **-Dr**\ *file* - 从文件中读取台站或接收点深度，文件每行包含一个数值

设置多个震源深度和台站或接收点深度后，程序会遍历二者的全部组合分别计算，
并分别保存或写入对应的计算结果。
