.. _-C:

**-C**\ *faultparam*
    读取 `Coulomb 程序 <https://pubs.usgs.gov/of/2011/1060/>`_ 格式的有限断层。
    *faultparam* 通常是有限断层文件的路径名，文件名后缀不参与输入格式判断。

    文件的前两行是 Coulomb 断层表头。第一行首个 token 必须为 ``#``，表示 **Index** 列；
    其后按顺序给出其余 10 个字段标签。第二行给出对应的 11 个占位字段；随后读取断层记录。
    每条记录包含 11 列，依次为：

    #. **Index**：索引号，PyGRT 不使用该列
    #. **X-start**：断层起点的 X 坐标
    #. **Y-start**：断层起点的 Y 坐标
    #. **X-fin**：断层终点的 X 坐标
    #. **Y-fin**：断层终点的 Y 坐标
    #. **Kode**：断层震源类型标识符（只能设置特定值）
    #. **value1**：第 7 列；在第 7 列表头精确写为 *rake* 时表示 *rake*，否则由 **Kode** 决定物理意义
    #. **value2**：第 8 列；在第 7 列表头精确写为 *rake* 时表示 *net slip*，否则由 **Kode** 决定物理意义
    #. **dip**：断层倾角，单位为 °
    #. **top**：断层顶深，单位为 km
    #. **bot**：断层底深，单位为 km

    X/Y 坐标的单位为 km，其中 X 对应 PyGRT 的 East 坐标，Y 对应 North 坐标
    （注意这个定义和 :doc:`/Module/static_syn` 中的 X/Y 方向相反）。
    起点到终点定义断层走向，断层长度由两点间距离计算。
    **top** 和 **bot** 是向下为正的深度，断层宽度由底深、顶深和倾角计算。
    倾角必须在 (0, 90]° 内，且底深必须大于顶深。

    第 7 个数据列的表头必须区分大小写并完整匹配 *rake*，才按 *rake* (°) 和
    *net slip* (m) 解释第 7、8 列；其他表头内容按分量格式解释。Coulomb 常见表头中，
    首列用 ``#`` 表示索引列，*dip angle* 可能由两个空白分隔的词组成，但数据列仍为 11 列。
    如果表头标识与文件名 *.inp* 或 *.inr* 不一致，程序会给出警告，但仍以表头标识为准。

    在分量格式下，第 6 列 **Kode** 决定第 7、8 列的物理意义：

    + **100**：矩形断层，**value1** 为右旋滑动、**value2** 为逆冲滑动，单位均为 m
    + **200**：矩形断层，**value1** 为右旋滑动、**value2** 为张裂开度，单位均为 m
    + **300**：矩形断层，**value1** 为张裂开度、**value2** 为逆冲滑动，单位均为 m
    + **400**：断层面中心的点双力偶源，**value1** 和 **value2** 分别为右旋滑动和逆冲滑动 *potency*，单位均为 m\ :sup:`3`
    + **500**：断层面中心的点张裂/膨胀源，**value1** 和 **value2** 分别为张裂 *potency* 和膨胀 *potency*，单位均为 m\ :sup:`3`

    对于 **Kode=100**、**200** 和 **300**，一条记录表示一个矩形断层；
    对于 **Kode=400** 和 **500**，上述几何列仍用于确定点源所在的
    断层面中心。

    当表头第 7 列为 *rake* 时只允许使用 **Kode=100**，第 7、8 列分别表示 *rake* (°)
    和 *net slip* (m)。如果需要使用 **Kode=200** 或 **Kode=300**，应使用分量格式表头。

    在 :doc:`/Module/okada` 模块中，计算结果为解析解， *faultparam* 只能是路径名，
    不能附加子断层设置。而在其他模块中， *faultparam* 还可以写成
    *path*\ **+i**\ *dL/dW*，例如 *faults.inp+i1/1*。其中 *dL* 和 *dW*
    分别是沿走向和沿倾向的子断层尺寸，单位为 km。该设置只用于将
    **Kode=100**、**200** 和 **300** 的矩形断层划分为子断层；
    **Kode=400** 和 **500** 始终作为一个点源处理。
    在 **static_syn** 模块中，如果省略 **+i**，则 *dL* 和 *dW* 均取格林函数库中
    震中距、震源深度和接收深度采样间隔的最小正值。
