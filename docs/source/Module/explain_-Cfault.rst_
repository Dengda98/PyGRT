.. _-C:

**-C**\ *faultparam*
    读取 `Coulomb 程序 <https://pubs.usgs.gov/of/2011/1060/>`_ 格式的有限断层。
    *faultparam* 通常是有限断层文件的路径名，支持 *.inp* 和 *.inr* 后缀。

    文件的前两行仅供用户阅读，程序固定跳过这两行，不检查表头内容，
    然后读取其后的断层记录。每条记录包含 11 列，依次为：

    #. **Index**：索引号，PyGRT 不使用该列
    #. **X-start**：断层起点的 X 坐标
    #. **Y-start**：断层起点的 Y 坐标
    #. **X-fin**：断层终点的 X 坐标
    #. **Y-fin**：断层终点的 Y 坐标
    #. **Kode**：断层震源类型标识符（只能设置特定值）
    #. **slip1**：由 **Kode** 决定物理意义的第一个震源量
    #. **slip2**：由 **Kode** 决定物理意义的第二个震源量
    #. **dip**：断层倾角，单位为 °
    #. **top**：断层顶深，单位为 km
    #. **bot**：断层底深，单位为 km

    X/Y 坐标的单位为 km，其中 X 对应 PyGRT 的 East 坐标，Y 对应 North 坐标
    （注意这个定义和 :doc:`/Module/static_syn` 中的 X/Y 方向相反）。
    起点到终点定义断层走向，断层长度由两点间距离计算。
    **top** 和 **bot** 是向下为正的深度，断层宽度由底深、顶深和倾角计算。
    倾角必须在 (0, 90]° 内，且底深必须大于顶深。

    第 6 列 **Kode** 决定第 7、8 列的物理意义：

    + **100**：矩形断层，**slip1** 为右旋滑动、**slip2** 为逆冲滑动，单位均为 m
    + **200**：矩形断层，**slip1** 为右旋滑动、**slip2** 为张裂开度，单位均为 m
    + **300**：矩形断层，**slip1** 为张裂开度、**slip2** 为逆冲滑动，单位均为 m
    + **400**：断层面中心的点双力偶源，**slip1** 和 **slip2** 分别为走向滑动和倾向滑动 *potency*，单位均为 m\ :sup:`3`
    + **500**：断层面中心的点张裂/膨胀源，**slip1** 和 **slip2** 分别为张裂 *potency* 和膨胀 *potency*，单位均为 m\ :sup:`3`

    对于 **Kode=100**、**200** 和 **300**，一条记录表示一个矩形断层；
    对于 **Kode=400** 和 **500**，上述几何列仍用于确定点源所在的
    断层面中心。

    *.inr* 文件只允许使用 **Kode=100**，第 7、8 列分别改为 *rake* (°)
    和 *net slip* (m)。如果需要使用 **Kode=200** 或 **Kode=300**，应使用
    *.inp* 文件。

    在 :doc:`/Module/okada` 模块中，计算结果为解析解， *faultparam* 只能是路径名，
    不能附加子断层设置。而在其他模块中， *faultparam* 还可以写成
    *path*\ **+i**\ *dL/dW*，例如 *faults.inp+i1/1*。其中 *dL* 和 *dW*
    分别是沿走向和沿倾向的子断层尺寸，单位为 km。该设置只用于将
    **Kode=100**、**200** 和 **300** 的矩形断层划分为子断层；
    **Kode=400** 和 **500** 始终作为一个点源处理。
