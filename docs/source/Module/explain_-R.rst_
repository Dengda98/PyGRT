.. _-R:

**-R**\ *r1/r2/dr*\|\ *r1,r2,...*\|\ *file*
    多个震中距 (km)。有三种设置方式。震中距不能为负数；显式列出的震中距必须严格递增：
    
    + **-R**\ *r1/r2/dr* - 在 [*r1, r2*] 范围内设置间隔为 *dr* 的一系列等距点。
    + **-R**\ *r1,r2,...* - 逗号分隔的多个震中距
    + **-R**\ *file* - 仅含一列震中距数值的文件 *file* 。
