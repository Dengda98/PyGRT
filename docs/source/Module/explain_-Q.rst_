.. _-Q:

**-Q**\ *file*
    从 ASCII 文件读取任意接收点。
    每行可以是北向坐标、东向坐标和接收深度 (km) 三列，
    也可以在其后增加走向、倾角和滑动角 (degree) 三列（共六列）。
    以 ``#`` 开头的行作为注释，所有数据行必须使用相同的列数。

    此时输出文件使用 ``points`` 布局，并保留每个接收点的 ``north``、``east``
    和 ``depth`` 变量。
    如果输入了后三列，输出文件还会在 ``point`` 维度下记录 ``strike``、``dip``
    和 ``rake`` 三个变量；这三个参数将作为接收断层形态用于后续计算。
    
    该选项与 **-X**、**-Y** 和 **-Dr** 互斥。
