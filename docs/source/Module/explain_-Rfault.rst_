.. _-R:

**-R**\ *faultparam*
    读取 `Coulomb 程序 <https://pubs.usgs.gov/of/2011/1060/>`_ 格式的有限断层作为接收点。
    后可跟 **+i**\ *dL/dW* 指定子断层尺度。
    在 **static_syn** 模块中，不设置 **+i** 时，沿走向和倾向的剖分尺寸均取格林函数库中
    震中距、震源深度和接收深度采样间隔的最小正值。此时每条有限断层会按照该尺寸划分为多个子断层。
    设置 **+i** 后，程序分别沿走向和倾向剖分，每个子断层生成一个位于子断层中心的接收点。
    末端子断层可以短于 *dL* 或 *dW*，输出的变量 **stksize** 和 **dipsize**
    保存每条有限断层沿走向和倾向的子断层数量。

    **okada** 模块没有格林函数库，因此不设置 **+i** 时仍将每条有限断层作为一个整体，
    需要剖分时应显式设置 **+i**。

    **-R** 与 **-Q**、**-X**、**-Y**、**-Dr** 互斥。接收点深度由断层几何确定，
    因此不能再设置统一的 **-Dr**。文件中的 kode 和滑动量不参与接收点位移的计算，
    但程序会保留每条有限断层的走向、倾角和滑动角。若滑动方向未定义，
    **rake** 使用 -999 表示。

    此时输出文件的全局 **layout** 属性为 **points**，所有接收点结果统一使用
    **point** 维度的一维变量，并通过 **nfault** 和 **offset** 补充有限断层分组信息：

    * **nfault**：有限断层数量
    * **strike(nfault)**、**dip(nfault)** 和 **rake(nfault)**：断层级几何
    * **offset(nfault)**：每条有限断层对应点范围的排他性结束索引，
      第一个断层的范围为 *[0, offset[0])*，第 *N* 个断层的范围为
      *[offset[N-2], offset[N-1])*，因此最后一个值为 **point**
    * **stksize(nfault)**：每条有限断层沿走向的子断层数量
    * **dipsize(nfault)**：每条有限断层沿倾向的子断层数量
